--========================
-- HIRUZ HUB
--========================

local Library = loadstring(game:HttpGet("https://pastefy.app/21Kp3vvz/raw"))()

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local Window = Library:CreateWindow({
    Title = "HiruZ Hub",
    Subtitle = ": b l o x f r u i t",
    Image = "rbxassetid://128185233852701"
})

--========================
-- TABS
--========================

local ShopTab = Window:AddTab("Shop")
local StatusTab = Window:AddTab("Status And Sever")
local LocalTab = Window:AddTab("Localphayer")
local SettingFarmTab = Window:AddTab("Settings Fram")
local SkillTab = Window:AddTab("Hold And Select Skill")
local FarmTab = Window:AddTab("Framing")
local StackTab = Window:AddTab("Stack Framing")
local FarmOtherTab = Window:AddTab("Framing Other")
local FruitTab = Window:AddTab("Fruit And Raid Dungeor")
local SeaTab = Window:AddTab("Sea Even")
local RaceTab = Window:AddTab("Upgrade Race")
local ItemTab = Window:AddTab("Get And Upgrade Item")
local ESPTab = Window:AddTab("ESP")
local PVPTab = Window:AddTab("PVP")

--========================
-- GROUPBOXES
--========================

-- Shop
local ShopGroup = ShopTab:AddLeftGroupbox("Shop")
local FightingGroup = ShopTab:AddRightGroupbox("Fighting Shop")
local AbilityGroup = ShopTab:AddRightGroupbox("Ability Shop")

-- Status
local StatusGroup = StatusTab:AddLeftGroupbox("Player Status")
local ServerGroup = StatusTab:AddRightGroupbox("Server")

-- Local Player
local LocalGroup = LocalTab:AddLeftGroupbox("Local Player")

-- Settings Farm
local SettingFarmGroup = SettingFarmTab:AddLeftGroupbox("Settings Fram")

-- Skill
local SkillGroup = SkillTab:AddLeftGroupbox("Hold And Select Skill")

-- Farming
local FarmGroup = FarmTab:AddLeftGroupbox("Framing")

-- Stack Farming
local StackGroup = StackTab:AddLeftGroupbox("Stack Framing")

-- Farming Other
local FarmOtherGroup = FarmOtherTab:AddLeftGroupbox("Framing Other")

-- Fruit / Raid
local FruitGroup = FruitTab:AddLeftGroupbox("Fruit")
local RaidGroup = FruitTab:AddRightGroupbox("Raid")

-- Sea
local SeaGroup = SeaTab:AddLeftGroupbox("Sea Even")

-- Race
local RaceGroup = RaceTab:AddLeftGroupbox("Upgrade Race")

-- Item
local ItemGroup = ItemTab:AddLeftGroupbox("Get And Upgrade Item")

-- ESP
local ESPGroup = ESPTab:AddLeftGroupbox("ESP")

-- PVP
local PVPGroup = PVPTab:AddLeftGroupbox("PVP")

--========================
-- SHOP
--========================

local ShopButtons = {
    "Redeem Code",
    "Teleport Old World",
    "Teleport Third Sea",
    "Buy Dual Flintlock",
    "Reroll Race",
    "Buy Ghoul Race",
    "Buy Cyborg Race"
}

for _, v in pairs(ShopButtons) do
    ShopGroup:AddButton({
        Text = v,

        Func = function()
            print(v)
        end
    })
end

--========================
-- FIGHTING SHOP
--========================

local FightingStyles = {
    "Black Leg",
    "Fishman Karate",
    "Electro",
    "Super Human",
    "Death Step",
    "Sharkman Karate",
    "Electric Claw",
    "Godhuman",
    "Sanguine Art"
}

for _, v in pairs(FightingStyles) do
    FightingGroup:AddToggle(v, {
        Text = v,
        Default = false,

        Callback = function(Value)
            print(v, Value)
        end
    })
end

--========================
-- ABILITY SHOP
--========================

local AbilityShop = {
    "Skyjump [$10,000 Beli]",
    "Buso Haki [$25,000 Beli]",
    "Observation Haki [$750,000 Beli]",
    "Soru [$100,000 Beli]"
}

for _, v in pairs(AbilityShop) do
    AbilityGroup:AddButton({
        Text = v,

        Func = function()
            print(v)
        end
    })
end

--========================
-- STATUS AND SEVER
--========================

StatusGroup:AddLabel(os.date("%d/%m/%Y - %H:%M:%S [VN]"))

StatusGroup:AddLabel("Hour : 00 | Minute : 00 | Second : 60")

StatusGroup:AddDivider()

StatusGroup:AddLabel("Mirage Island : Not Spawn")

StatusGroup:AddLabel("Kitsune Island : Not Spawn")

StatusGroup:AddLabel("Prehistoric Island : Not Spawn")

StatusGroup:AddLabel("Frozen Dimension : Not Spawn")

StatusGroup:AddDivider()

StatusGroup:AddLabel("Kill : 93")

StatusGroup:AddLabel("Rip_Indra : Alive")

StatusGroup:AddLabel("Dough King : Alive")

StatusGroup:AddLabel("Elite Hunter : [93 Kill]")

StatusGroup:AddLabel("Full Moon : 0/5")

--========================
-- SERVER
--========================

ServerGroup:AddButton({
    Text = "Hop Server",

    Func = function()
        print("Hop Server")
    end
})

ServerGroup:AddButton({
    Text = "Rejoin Server",

    Func = function()
        print("Rejoin Server")
    end
})

ServerGroup:AddButton({
    Text = "Copy JobId",

    Func = function()
        setclipboard(game.JobId)
    end
})

ServerGroup:AddToggle("AutoHop", {
    Text = "Auto Hop",
    Default = false,

    Callback = function(Value)
        print(Value)
    end
})

--========================
-- LOCAL PLAYER
--========================

LocalGroup:AddSlider("WalkSpeed", {
    Text = "Walk Speed",
    Min = 16,
    Max = 200,
    Default = 16,
    Rounding = 0,

    Callback = function(Value)
        print(Value)
    end
})

LocalGroup:AddSlider("JumpPower", {
    Text = "Jump Power",
    Min = 50,
    Max = 300,
    Default = 50,
    Rounding = 0,

    Callback = function(Value)
        print(Value)
    end
})

LocalGroup:AddToggle("NoClip", {
    Text = "No Clip",
    Default = false,

    Callback = function(Value)
        print(Value)
    end
})

--========================
-- SETTINGS FARM
--========================

SettingFarmGroup:AddToggle("AutoHaki", {
    Text = "Auto Haki",
    Default = false,

    Callback = function(Value)
        print(Value)
    end
})

SettingFarmGroup:AddToggle("FastAttack", {
    Text = "Fast Attack",
    Default = false,

    Callback = function(Value)
        print(Value)
    end
})

SettingFarmGroup:AddSlider("FarmDistance", {
    Text = "Distance Farm Aura",
    Min = 0,
    Max = 500,
    Default = 300,
    Rounding = 0,

    Callback = function(Value)
        print(Value)
    end
})

--========================
-- HOLD AND SELECT SKILL
--========================

SkillGroup:AddDropdown("SelectSkill", {
    Values = {"Z", "X", "C", "V", "F"},
    Default = 1,
    Multi = true,

    Text = "Select Skill",

    Callback = function(Value)
        print(Value)
    end
})

--========================
-- FARMING
--========================

FarmGroup:AddToggle("AutoFarm", {
    Text = "Auto Farm",
    Default = false,

    Callback = function(Value)
        print(Value)
    end
})

FarmGroup:AddToggle("AutoQuest", {
    Text = "Auto Quest",
    Default = false,

    Callback = function(Value)
        print(Value)
    end
})

FarmGroup:AddDropdown("SelectWeapon", {
    Values = {"Melee", "Sword", "Gun", "Blox Fruit"},
    Default = 1,
    Multi = false,

    Text = "Select Weapon",

    Callback = function(Value)
        print(Value)
    end
})

--========================
-- STACK FARMING
--========================

StackGroup:AddToggle("StackMob", {
    Text = "Stack Mob",
    Default = false,

    Callback = function(Value)
        print(Value)
    end
})

StackGroup:AddSlider("StackDistance", {
    Text = "Stack Distance",
    Min = 0,
    Max = 100,
    Default = 25,
    Rounding = 0,

    Callback = function(Value)
        print(Value)
    end
})

--========================
-- FARMING OTHER
--========================

FarmOtherGroup:AddToggle("BringMob", {
    Text = "Bring Mob",
    Default = false,

    Callback = function(Value)
        print(Value)
    end
})

FarmOtherGroup:AddToggle("AutoChest", {
    Text = "Auto Chest",
    Default = false,

    Callback = function(Value)
        print(Value)
    end
})

FarmOtherGroup:AddToggle("TweenFarm", {
    Text = "Tween Farm",
    Default = false,

    Callback = function(Value)
        print(Value)
    end
})

--========================
-- FRUIT
--========================

FruitGroup:AddToggle("AutoFruit", {
    Text = "Auto Collect Fruit",
    Default = false,

    Callback = function(Value)
        print(Value)
    end
})

FruitGroup:AddToggle("FruitESP", {
    Text = "Fruit ESP",
    Default = false,

    Callback = function(Value)
        print(Value)
    end
})

--========================
-- RAID
--========================

RaidGroup:AddToggle("AutoRaid", {
    Text = "Auto Raid",
    Default = false,

    Callback = function(Value)
        print(Value)
    end
})

RaidGroup:AddDropdown("SelectRaid", {
    Values = {
        "Flame",
        "Ice",
        "Dark",
        "Light",
        "Magma",
        "Buddha",
        "Dough"
    },

    Default = 1,
    Multi = false,

    Text = "Select Raid",

    Callback = function(Value)
        print(Value)
    end
})

--========================
-- SEA EVENT
--========================

SeaGroup:AddToggle("SeaEvent", {
    Text = "Auto Sea Event",
    Default = false,

    Callback = function(Value)
        print(Value)
    end
})

SeaGroup:AddToggle("SharkHunter", {
    Text = "Shark Hunter",
    Default = false,

    Callback = function(Value)
        print(Value)
    end
})

SeaGroup:AddToggle("TerrorShark", {
    Text = "Terror Shark",
    Default = false,

    Callback = function(Value)
        print(Value)
    end
})

--========================
-- RACE
--========================

RaceGroup:AddToggle("AutoRaceV3", {
    Text = "Auto Race V3",
    Default = false,

    Callback = function(Value)
        print(Value)
    end
})

RaceGroup:AddToggle("AutoRaceV4", {
    Text = "Auto Race V4",
    Default = false,

    Callback = function(Value)
        print(Value)
    end
})

--========================
-- ITEM
--========================

ItemGroup:AddToggle("AutoCDK", {
    Text = "Auto CDK",
    Default = false,

    Callback = function(Value)
        print(Value)
    end
})

ItemGroup:AddToggle("AutoSoulGuitar", {
    Text = "Auto Soul Guitar",
    Default = false,

    Callback = function(Value)
        print(Value)
    end
})

--========================
-- ESP
--========================

ESPGroup:AddToggle("PlayerESP", {
    Text = "Player ESP",
    Default = false,

    Callback = function(Value)
        print(Value)
    end
})

ESPGroup:AddToggle("ChestESP", {
    Text = "Chest ESP",
    Default = false,

    Callback = function(Value)
        print(Value)
    end
})

ESPGroup:AddToggle("IslandESP", {
    Text = "Island ESP",
    Default = false,

    Callback = function(Value)
        print(Value)
    end
})

--========================
-- PVP
--========================

PVPGroup:AddToggle("AttackAura", {
    Text = "Attack Aura",
    Default = false,

    Callback = function(Value)
        print(Value)
    end
})

PVPGroup:AddToggle("AutoSkill", {
    Text = "Auto Skill",
    Default = false,

    Callback = function(Value)
        print(Value)
    end
})

PVPGroup:AddDropdown("PVPWeapon", {
    Values = {"Melee", "Sword", "Gun", "Blox Fruit"},
    Default = 1,
    Multi = false,

    Text = "Select Weapon",

    Callback = function(Value)
        print(Value)
    end
})

--========================
-- NOTIFY
--========================

Library:Notify({
    Title = "HiruZ Loaded",
    Description = "Hub Loaded Successfully",
    Duration = 5
})
