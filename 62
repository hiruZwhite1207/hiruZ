-- [[ hiruZ Hub - Blox Fruits Edition ]] --
-- UI Style: Linoria / Banana (Đã FIX CHUẨN MÀU Deep Blue Gaming)
-- Core Engine: Optimized Rubu V6 (No UI Overlap Bug)

local Library = loadstring(game:HttpGet("https://pastefy.app/21Kp3vvz/raw"))()

local Window = Library:CreateWindow({
    Title = "HiruZ Hub",
    Subtitle = ": b l o x f r u i t",
    Image = "rbxassetid://128185233852701"
})

-- ====================================================================
-- CUSTOM THEME: XANH BIỂN ĐẬM (DEEP BLUE GAMING)
-- ====================================================================
if Library.Theme then
    Library.Theme.BackgroundColor = Color3.fromRGB(8, 12, 22)
    Library.Theme.MainColor = Color3.fromRGB(14, 20, 32)
    Library.Theme.AccentColor = Color3.fromRGB(0, 102, 255) -- Xanh biển sáng làm điểm nhấn
    Library.Theme.OutlineColor = Color3.fromRGB(24, 34, 54)
    Library.Theme.FontColor = Color3.fromRGB(255, 255, 255)
end

-- Lệnh ép UI Lib phải refresh và áp dụng bảng màu mới ngay lập tức
if Library.UpdateTheme then
    Library:UpdateTheme()
elseif Library.UpdateColorsUsingRegistry then
    Library:UpdateColorsUsingRegistry()
end

-- ====================================================================
-- KHỞI TẠO CÁC CẤU HÌNH TRẠNG THÁI (VARIABLES)
-- ====================================================================
_G.KillAura = false
_G.AttackEnemies = false
_G.AttackPlayers = false
_G.WalkSpeed = 16
_G.JumpPower = 50
_G.AttackSpeed = 0.019
_G.AttackRadius = 60
_G.Animation = true
_G.Seriality = true

--====================================================================
-- KHỞI TẠO DANH SÁCH TABS CỦA HIRUZ HUB
--====================================================================
local ShopTab = Window:AddTab("Shop Tab")
local ConfigTab = Window:AddTab("Config Farm Tab")
local FarmingTab = Window:AddTab("Farmming Tab")
local StackTab = Window:AddTab("Stack Farmming Tab")
local ItemTab = Window:AddTab("Item Farmming Tab")
local LocalTab = Window:AddTab("LocalPlayer Tab")
local FruitDungeonTab = Window:AddTab("Fruits & Dungeon")
local RaceTab = Window:AddTab("Upgraded Race Tab")
local SettingsTab = Window:AddTab("Settings Tab")

-- Nhóm UI tương ứng
local ShopGroup = ShopTab:AddLeftGroupbox("Haki & Melee")
local FarmGroup = FarmingTab:AddLeftGroupbox("Auto Farmming Engine")
local LocalGroup = LocalTab:AddLeftGroupbox("Player Modifications")
local SettingsGroup = SettingsTab:AddLeftGroupbox("Script Configuration")

--====================================================================
-- CHỨC NĂNG 1: TỰ ĐỘNG NHẬP CODE (TAB SHOP)
--====================================================================
ShopGroup:AddButton({
    Text = "Redeem All Codes",
    Func = function()
        local codes = {"REWARDCODEX", "NEWCODE", "KITTGAMING", "SUB2GAMERROBOT_EXP1"}
        local remote = game:GetService("ReplicatedStorage"):FindFirstChild("Remotes") and game:GetService("ReplicatedStorage").Remotes:FindFirstChild("RedeemCode")
        
        if remote then
            for _, code in pairs(codes) do
                remote:InvokeServer(code)
                task.wait(0.3)
            end
            Library:Notify({Title = "hiruZ Hub", Description = "Đã chạy lệnh nhập toàn bộ code!", Duration = 3})
        else
            Library:Notify({Title = "Thất bại", Description = "Không tìm thấy Remote nhập code", Duration = 3})
        end
    end
})

--====================================================================
-- CHỨC NĂNG 2: AUTO FARM & ATTACK AURA (TAB FARMMING)
--====================================================================
FarmGroup:AddToggle("FarmEnemiesToggle", {
    Text = "Auto Farm Level (Quét Quái)",
    Default = false,
    Callback = function(Value)
        _G.KillAura = Value
        _G.AttackEnemies = Value
    end
})

FarmGroup:AddToggle("FarmPlayersToggle", {
    Text = "Attack Aura (Săn Người Chơi)",
    Default = false,
    Callback = function(Value)
        _G.KillAura = Value
        _G.AttackPlayers = Value
    end
})

--====================================================================
-- CHỨC NĂNG 3: HACK LOCALPLAYER SPEED/JUMP (TAB LOCALPLAYER)
--====================================================================
LocalGroup:AddSlider("SpeedSlider", {
    Text = "Tốc độ chạy (WalkSpeed)",
    Default = 16, Min = 16, Max = 250, Rounding = 0,
    Callback = function(Value)
        _G.WalkSpeed = Value
    end
})

LocalGroup:AddSlider("JumpSlider", {
    Text = "Sức nhảy (JumpPower)",
    Default = 50, Min = 50, Max = 300, Rounding = 0,
    Callback = function(Value)
        _G.JumpPower = Value
    end
})

task.spawn(function()
    while true do
        task.wait(0.1)
        pcall(function()
            local character = game:GetService("Players").LocalPlayer.Character
            if character and character:FindFirstChildOfClass("Humanoid") then
                character.Humanoid.WalkSpeed = _G.WalkSpeed
                character.Humanoid.JumpPower = _G.JumpPower
            end
        end)
    end
end)

--====================================================================
-- CHỨC NĂNG 4: CÀI ĐẶT CẤU HÌNH HACK (TAB SETTINGS)
--====================================================================
SettingsGroup:AddSlider("AtkRadius", {
    Text = "Bán kính đấm lan",
    Default = 60, Min = 10, Max = 100, Rounding = 0,
    Callback = function(v) _G.AttackRadius = v end
})

SettingsGroup:AddSlider("AtkDelay", {
    Text = "Tốc độ Delay Đấm",
    Default = 0.019, Min = 0.01, Max = 0.1, Rounding = 3,
    Callback = function(v) _G.AttackSpeed = v end
})

-- ====================================================================
-- CORE ENGINE ATTACK (BÓC TÁCH TỪ RUBU V6 - KHÔNG BỊ ĐÈ UI)
-- ====================================================================
local P = game:GetService("Players")
local L = P.LocalPlayer
local RS = game:GetService("ReplicatedStorage")
local W = workspace
local M = RS:WaitForChild("Modules")
local CU = pcall(require, M:WaitForChild("CombatUtil")) and require(M.CombatUtil) or nil
local WD = pcall(require, M:WaitForChild("WeaponData")) and require(M.WeaponData) or nil

if not CU then return end

local N = M:FindFirstChild("Net")
local RA = N and (N:FindFirstChild("RE/RegisterAttack") or N:FindFirstChild("RegisterAttack"))
local RH = N and (N:FindFirstChild("RE/RegisterHit") or N:FindFirstChild("RegisterHit"))	
local IS

do
    local PS = L:WaitForChild("PlayerScripts")
    for _, s in next, PS:GetChildren() do
        if s:IsA("LocalScript") then
            local ok, env = pcall(getsenv, s)
            if ok and env and env._G and typeof(env._G.SendHitsToServer) == "function" then
                IS = env._G.SendHitsToServer
                break
            end
        end
    end
end

pcall(function()
    hookfunction(CU.GetComboPaddingTime, function() return 0 end)
    hookfunction(CU.GetAttackCancelMultiplier, function() return 0 end)
    hookfunction(CU.CanAttack, function() return true end)
end)

local HList = {"RightLowerArm", "RightUpperArm", "LeftLowerArm", "LeftUpperArm", "RightHand", "LeftHand", "HumanoidRootPart", "Head", "UpperTorso", "LowerTorso"}

local function okm(m)
    local h = m:FindFirstChildWhichIsA("Humanoid")
    return h and h.Health > 0 and m:FindFirstChild("HumanoidRootPart") and not m:FindFirstChild("VehicleSeat")
end

local function hpt(m)
    local p = m:FindFirstChild(HList[math.random(1, #HList)])
    return p or m:FindFirstChild("HumanoidRootPart")
end

local function near(r, maxN)
    local out, ch = {}, L.Character
    if not ch or not ch:FindFirstChild("HumanoidRootPart") then return out end
    local p0 = ch.HumanoidRootPart.Position

    if _G.AttackEnemies then
        for _, grp in next, {W:FindFirstChild("Enemies"), W:FindFirstChild("Characters")} do
            if grp then
                for _, v in next, grp:GetChildren() do
                    if #out >= maxN then break end
                    if v ~= ch and okm(v) and not P:GetPlayerFromCharacter(v) then
                        local hr = v:FindFirstChild("HumanoidRootPart")
                        if hr and (hr.Position - p0).Magnitude <= r then
                            out[#out + 1] = v
                        end
                    end
                end
            end
        end
    end

    if _G.AttackPlayers then
        for _, pl in next, P:GetPlayers() do
            if #out >= maxN then break end
            if pl ~= L and pl.Character and okm(pl.Character) then
                local hr = pl.Character:FindFirstChild("HumanoidRootPart")
                if hr and (hr.Position - p0).Magnitude <= r then
                    out[#out + 1] = pl.Character
                end
            end
        end
    end
    return out
end

local function pkg(t)
    local main, hits = nil, {}
    for _, v in next, t do
        if okm(v) then
            local p = hpt(v)
            if p then
                if not main then main = p end
                hits[#hits + 1] = {v, p}
            end
        end
    end
    return main, hits
end

local function send(main, hits)
    if main and #hits > 0 then
        if IS then IS(main, hits) elseif RH then RH:FireServer(main, hits) end
    end
end

task.spawn(function()
    while true do
        task.wait(_G.AttackSpeed)
        if _G.KillAura then
            pcall(function()
                local ch = L.Character
                if not ch or not ch:FindFirstChildOfClass("Tool") then return end
                local tool = ch:FindFirstChildOfClass("Tool")

                local tg = near(_G.AttackRadius, 20)
                if #tg == 0 then return end

                local main, hits = pkg(tg)
                if not main then return end

                if RA then RA:FireServer(0) end
                
                if _G.Seriality and tool.ToolTip == "Blox Fruit" then
                    local LeftClickRemote = tool:FindFirstChild('LeftClickRemote')
                    if LeftClickRemote then
                        LeftClickRemote:FireServer(Vector3.new(0.01, -500, 0.01), 1, true)
                        LeftClickRemote:FireServer(false)
                    end
                end

                task.defer(function()
                    pcall(function()
                        CU:AttackStart(main, 1)
                        CU:RunHitDetection(main.Parent or main, 1, {_Object = {Length = 0.02, IsPlaying = true}})
                    end)
                end)
                send(main, hits)
            end)
        end
    end
end)

--====================================================================
-- THÔNG BÁO HOÀN THÀNH
--====================================================================
Library:Notify({
    Title = "hiruZ Hub Loaded!",
    Description = "Welcome to hiruZ Hub - Deep Blue Version.",
    Duration = 5
})
