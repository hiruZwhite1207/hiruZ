local Fluent = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/source.lua"))()
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TeleportService = game:GetService("TeleportService")
local VirtualUser = game:GetService("VirtualUser")

local LocalPlayer = Players.LocalPlayer

-- INTRO
local Intro = Instance.new("ScreenGui")
local Frame = Instance.new("Frame")
local Title = Instance.new("TextLabel")

Intro.Name = "HiruzIntro"
pcall(function()
    Intro.Parent = game.CoreGui
end)

Frame.Parent = Intro
Frame.BackgroundColor3 = Color3.fromRGB(10,10,10)
Frame.Size = UDim2.new(1,0,1,0)

Title.Parent = Frame
Title.BackgroundTransparency = 1
Title.Size = UDim2.new(1,0,1,0)
Title.Font = Enum.Font.GothamBold
Title.Text = "HIRUZ // BLADE"
Title.TextColor3 = Color3.fromRGB(255,120,120)
Title.TextScaled = true
Title.TextTransparency = 1

TweenService:Create(
    Title,
    TweenInfo.new(1),
    {TextTransparency = 0}
):Play()

task.wait(2)

TweenService:Create(
    Frame,
    TweenInfo.new(1),
    {BackgroundTransparency = 1}
):Play()

TweenService:Create(
    Title,
    TweenInfo.new(1),
    {TextTransparency = 1}
):Play()

task.wait(1)

Intro:Destroy()

-- WINDOW
local Window = Fluent:CreateWindow({
    Title = "HIRUZ HUB",
    SubTitle = "Blade Ball",
    TabWidth = 160,
    Size = UDim2.fromOffset(540, 420),
    Acrylic = true,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl
})

-- TABS
local Tabs = {
    Combat = Window:AddTab({
        Title = "Combat",
        Icon = "sword"
    }),

    Visual = Window:AddTab({
        Title = "Visual",
        Icon = "eye"
    }),

    Player = Window:AddTab({
        Title = "Player",
        Icon = "user"
    }),

    Misc = Window:AddTab({
        Title = "Misc",
        Icon = "settings"
    }),

    Settings = Window:AddTab({
        Title = "Settings",
        Icon = "cog"
    })
}

-- VARIABLES
local AutoParry = false
local AutoSpam = false
local LegitMode = false
local BallESP = false
local TargetESP = false
local BallBait = false
local HitSound = false

_G.ParryDistance = 15
_G.BaitDistance = 12
_G.ReactionDelay = 0.03
_G.SpamSpeed = 3

-- NOTIFY
Fluent:Notify({
    Title = "HIRUZ HUB",
    Content = "Loaded successfully.",
    Duration = 4
})

------------------------------------------------
-- COMBAT
------------------------------------------------

Tabs.Combat:AddToggle("AutoParry", {
    Title = "Auto Parry",
    Default = false,
    Callback = function(v)
        AutoParry = v
    end
})

Tabs.Combat:AddToggle("AutoSpam", {
    Title = "Auto Spam",
    Default = false,
    Callback = function(v)
        AutoSpam = v
    end
})

Tabs.Combat:AddSlider("SpamSpeed", {
    Title = "Spam Speed",
    Default = 3,
    Min = 1,
    Max = 10,
    Rounding = 1,
    Callback = function(v)
        _G.SpamSpeed = v
    end
})

Tabs.Combat:AddToggle("LegitMode", {
    Title = "Legit Mode",
    Default = false,
    Callback = function(v)
        LegitMode = v
    end
})

Tabs.Combat:AddToggle("BallBait", {
    Title = "Ball Bait",
    Default = false,
    Callback = function(v)
        BallBait = v
    end
})

Tabs.Combat:AddSlider("ParryDistance", {
    Title = "Parry Distance",
    Default = 15,
    Min = 5,
    Max = 30,
    Rounding = 1,
    Callback = function(v)
        _G.ParryDistance = v
    end
})

Tabs.Combat:AddSlider("BaitDistance", {
    Title = "Safe Distance",
    Default = 12,
    Min = 5,
    Max = 25,
    Rounding = 1,
    Callback = function(v)
        _G.BaitDistance = v
    end
})

Tabs.Combat:AddSlider("ReactionDelay", {
    Title = "Reaction Delay",
    Default = 0.03,
    Min = 0,
    Max = 0.2,
    Rounding = 2,
    Callback = function(v)
        _G.ReactionDelay = v
    end
})

Tabs.Combat:AddToggle("HitSound", {
    Title = "Hit Sound",
    Default = false,
    Callback = function(v)
        HitSound = v
    end
})

------------------------------------------------
-- VISUAL
------------------------------------------------

Tabs.Visual:AddToggle("BallESP", {
    Title = "Ball ESP",
    Default = false,
    Callback = function(v)
        BallESP = v
    end
})

Tabs.Visual:AddToggle("TargetESP", {
    Title = "Target Highlight",
    Default = false,
    Callback = function(v)
        TargetESP = v
    end
})

------------------------------------------------
-- PLAYER
------------------------------------------------

Tabs.Player:AddSlider("WalkSpeed", {
    Title = "WalkSpeed",
    Default = 16,
    Min = 16,
    Max = 100,
    Rounding = 1,
    Callback = function(v)

        if LocalPlayer.Character and
           LocalPlayer.Character:FindFirstChild("Humanoid") then

            LocalPlayer.Character.Humanoid.WalkSpeed = v
        end
    end
})

Tabs.Player:AddSlider("JumpPower", {
    Title = "JumpPower",
    Default = 50,
    Min = 50,
    Max = 150,
    Rounding = 1,
    Callback = function(v)

        if LocalPlayer.Character and
           LocalPlayer.Character:FindFirstChild("Humanoid") then

            LocalPlayer.Character.Humanoid.JumpPower = v
        end
    end
})

Tabs.Player:AddSlider("FOV", {
    Title = "Field Of View",
    Default = 70,
    Min = 70,
    Max = 120,
    Rounding = 1,
    Callback = function(v)
        workspace.CurrentCamera.FieldOfView = v
    end
})

------------------------------------------------
-- MISC
------------------------------------------------

Tabs.Misc:AddButton({
    Title = "FPS Boost",
    Callback = function()

        for _,v in pairs(game:GetDescendants()) do

            if v:IsA("ParticleEmitter")
            or v:IsA("Trail")
            or v:IsA("Explosion") then

                v:Destroy()
            end
        end

        settings().Rendering.QualityLevel =
            Enum.QualityLevel.Level01

        Fluent:Notify({
            Title = "FPS Boost",
            Content = "Potato mode enabled 🥔",
            Duration = 3
        })
    end
})

Tabs.Misc:AddButton({
    Title = "Rejoin",
    Callback = function()
        TeleportService:Teleport(
            game.PlaceId,
            LocalPlayer
        )
    end
})

Tabs.Misc:AddButton({
    Title = "Anti AFK",
    Callback = function()

        LocalPlayer.Idled:Connect(function()

            VirtualUser:Button2Down(
                Vector2.new(0,0),
                workspace.CurrentCamera.CFrame
            )

            task.wait(1)

            VirtualUser:Button2Up(
                Vector2.new(0,0),
                workspace.CurrentCamera.CFrame
            )
        end)

        Fluent:Notify({
            Title = "Anti AFK",
            Content = "Enabled.",
            Duration = 3
        })
    end
})

------------------------------------------------
-- SAVE SYSTEM
------------------------------------------------

local SaveManager = loadstring(game:HttpGet(
"https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/SaveManager.lua"
))()

local InterfaceManager = loadstring(game:HttpGet(
"https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/InterfaceManager.lua"
))()

SaveManager:SetLibrary(Fluent)
InterfaceManager:SetLibrary(Fluent)

InterfaceManager:SetFolder("HIRUZ")
SaveManager:SetFolder("HIRUZ/BladeBall")

InterfaceManager:BuildInterfaceSection(Tabs.Settings)
SaveManager:BuildConfigSection(Tabs.Settings)

------------------------------------------------
-- MAIN LOOP
------------------------------------------------

RunService.RenderStepped:Connect(function()

    local Ball = workspace:FindFirstChild("Ball")

    if Ball and Ball:FindFirstChild("target") then

        local Character = LocalPlayer.Character

        if Character and Character:FindFirstChild("HumanoidRootPart") then

            local HRP = Character.HumanoidRootPart

            local Distance = (
                Ball.Position -
                HRP.Position
            ).Magnitude

            local Velocity =
                Ball.AssemblyLinearVelocity.Magnitude

            -- BALL ESP
            if BallESP then

                Ball.Material = Enum.Material.Neon
                Ball.Color = Color3.fromRGB(255,120,120)
            end

            -- TARGET CHECK
            if tostring(Ball.target.Value) ==
                LocalPlayer.Name then

                -- TARGET ESP
                if TargetESP then
                    HRP.Color = Color3.fromRGB(255,120,120)
                end

                local Remote =
                    ReplicatedStorage:FindFirstChild("Parry")

                if Remote then

                    ------------------------------------------------
                    -- BALL BAIT
                    ------------------------------------------------

                    if BallBait then

                        local DynamicDistance =
                            _G.BaitDistance +
                            math.clamp(Velocity / 40,0,8)

                        if Distance <= DynamicDistance then

                            task.wait(_G.ReactionDelay)

                            Remote:FireServer()

                            if HitSound then

                                local Sound =
                                    Instance.new("Sound")

                                Sound.SoundId =
                                    "rbxassetid://7147454322"

                                Sound.Volume = 2
                                Sound.Parent = workspace

                                Sound:Play()

                                game.Debris:AddItem(Sound,2)
                            end
                        end
                    end

                    ------------------------------------------------
                    -- NORMAL AUTO PARRY
                    ------------------------------------------------

                    if AutoParry and
                       Distance <= _G.ParryDistance then

                        if LegitMode then
                            task.wait(
                                math.random(10,25)/1000
                            )
                        end

                        Remote:FireServer()

                        if HitSound then

                            local Sound =
                                Instance.new("Sound")

                            Sound.SoundId =
                                "rbxassetid://7147454322"

                            Sound.Volume = 2
                            Sound.Parent = workspace

                            Sound:Play()

                            game.Debris:AddItem(Sound,2)
                        end
                    end

                    ------------------------------------------------
                    -- AUTO SPAM
                    ------------------------------------------------

                    if AutoSpam and Distance <= 20 then

                        for i = 1,_G.SpamSpeed do
                            Remote:FireServer()
                        end
                    end
                end
            end
        end
    end
end)
