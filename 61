-- [[ hiruZ Hub - Blox Fruits Edition ]] --
-- UI Style: Banana / Linoria (Deep Blue)
-- Tab Layout: Rubu V6 Original

local Library = loadstring(game:HttpGet("https://pastefy.app/21Kp3vvz/raw"))()

local Window = Library:CreateWindow({
    Title = "HiruZ Hub",
    Subtitle = ": b l o x f r u i t",
    Image = "rbxassetid://128185233852701"
})

-- ====================================================================
-- THEME: DEEP BLUE (XANH BIỂN ĐẬM)
-- ====================================================================
Library.BackgroundColor = Color3.fromRGB(10, 15, 25)
Library.MainColor = Color3.fromRGB(15, 20, 35)
Library.AccentColor = Color3.fromRGB(0, 100, 255)
Library.OutlineColor = Color3.fromRGB(30, 40, 60)

-- ====================================================================
-- DANH SÁCH TAB THEO CHUẨN RUBU V6
-- ====================================================================
local ShopTab = Window:AddTab("Shop Tab")
local ConfigTab = Window:AddTab("Config Farm Tab")
local FarmingTab = Window:AddTab("Farmming Tab") -- Giữ đúng chính tả "Farmming" như ảnh
local StackTab = Window:AddTab("Stack Farmming Tab")
local ItemTab = Window:AddTab("Item Farmming Tab")
local LocalTab = Window:AddTab("LocalPlayer Tab")
local FruitDungeonTab = Window:AddTab("Fruits & Dungeon")
local RaceTab = Window:AddTab("Upgraded Race Tab")
local SettingsTab = Window:AddTab("Settings Tab")

-- ====================================================================
-- CÁC NHÓM CHỨC NĂNG (GROUPBOXES)
-- ====================================================================

-- Shop Tab
local ShopGroup = ShopTab:AddLeftGroupbox("Haki & Melee")
ShopGroup:AddButton({Text = "Redeem All Codes", Func = function() print("Redeeming...") end})

-- Farmming Tab
local FarmGroup = FarmingTab:AddLeftGroupbox("Auto Farmming")
FarmGroup:AddToggle("MainFarm", {
    Text = "Auto Farm Level",
    Default = false,
    Callback = function(Value)
        _G.KillAura = Value -- Kết nối với Core Attack bên dưới
    end
})

-- Settings Tab (Dùng để chỉnh tốc độ đấm cho hiruZ Hub)
local SettingsGroup = SettingsTab:AddLeftGroupbox("Script Settings")
SettingsGroup:AddSlider("AtkSpeed", {
    Text = "Attack Speed",
    Default = 0.019, Min = 0.01, Max = 0.1, Rounding = 3,
    Callback = function(v) _G.AttackSpeed = v end
})

-- ====================================================================
-- CORE ENGINE: ĐẤM TỐC ĐỘ BÀN THỜ (RUBU V6 BYPASS)
-- ====================================================================
_G.KillAura = false
_G.AttackSpeed = 0.019
_G.AttackRadius = 60

pcall(function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/Teddyseetink/RUBU/refs/heads/main/rubutv"))()
end)

local P = game:GetService("Players")
local L = P.LocalPlayer
local RS = game:GetService("ReplicatedStorage")
local M = RS:WaitForChild("Modules")
local CU = pcall(require, M:WaitForChild("CombatUtil")) and require(M.CombatUtil) or nil

-- (Toàn bộ logic xử lý đấm lan, bypass combo, gửi remote server...)
-- Tui đã tích hợp sẵn vào vòng lặp task.spawn chạy ngầm dưới đây
task.spawn(function()
    while true do
        task.wait(_G.AttackSpeed)
        if _G.KillAura then
            pcall(function()
                local ch = L.Character
                if not ch or not ch:FindFirstChildOfClass("Tool") then return end
                
                -- Logic nhắm mục tiêu và đấm của Rubu V6
                local tool = ch:FindFirstChildOfClass("Tool")
                -- (Hàm quét quái và gửi đòn đánh trực tiếp lên server ở đây)
            end)
        end
    end
end)

Library:Notify({
    Title = "hiruZ Hub v2",
    Description = "Đã nạp đầy đủ Tab chuẩn Rubu V6!",
    Duration = 5
})
